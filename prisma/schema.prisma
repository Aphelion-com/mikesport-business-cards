generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Card {
  id              String   @id @default(cuid())
  fullName        String
  slug            String   @unique
  position        String
  department      String?
  companyName     String   @default("Mike Sport")
  officeLocation  String?
  description     String?
  mobilePhone     String
  companyPhone    String?
  extension       String?
  email           String
  website         String?
  address         String?
  profileImageUrl String?
  profileImageAlt String?
  linkedinUrl     String?
  instagramUrl    String?
  facebookUrl     String?
  tiktokUrl       String?
  displayOrder    Int      @default(0)
  isActive        Boolean  @default(true)
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  events CardEvent[]

  @@index([slug])
  @@map("cards")
}

enum CardEventType {
  PAGE_VIEW
  SAVE_CONTACT
  QR_DOWNLOAD
  COPY_URL
  PREVIEW_OPEN
}

model CardEvent {
  id        String        @id @default(cuid())
  cardId    String
  type      CardEventType
  userAgent String?
  referrer  String?
  createdAt DateTime      @default(now())

  card Card @relation(fields: [cardId], references: [id], onDelete: Cascade)

  @@index([cardId])
  @@index([type])
  @@index([createdAt])
  @@map("card_events")
}

model AppSettings {
  id                  String   @id @default("singleton")
  logoUrl             String?
  faviconUrl          String?
  dashboardTitle      String   @default("Mike Sport Cards")
  accentColor         String   @default("#F1582B")
  companyWebsite      String?
  defaultAddress      String?
  defaultCompanyPhone String?
  // Business card emblem (shown on public card pages)
  cardEmblemUrl       String?
  showEmblemOnCards   Boolean  @default(true)
  emblemPosition      String   @default("top") // top | header | footer
  updatedAt           DateTime @updatedAt @default(now())

  @@map("app_settings")
}
